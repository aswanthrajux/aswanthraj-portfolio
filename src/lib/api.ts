// Phase 2: Supabase API stubs
// These will be replaced with actual Supabase queries in Phase 2.
// For now, all content is served from static MDX files and TypeScript arrays.

export const supabase = null // will be: createClient(VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
